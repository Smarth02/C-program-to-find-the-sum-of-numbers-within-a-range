#include <stdio.h>
#include <stdlib.h>
void update(int *, int *);
int main()
{
    int a, b;
    scanf("%d%d", &a, &b);
    update(&a, &b);
    printf("%d\n%d", a, b);
    return 0;
}
void update(int *A, int *B){
    int a=*A;
    int b=*B;
    *A=a+b;
    *B=abs(a-b);
    
}
